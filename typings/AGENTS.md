# AGENTS — biology_textbook/typings

Status: REAL (doc fleet, 2026-08-30)

Type-stub bootstrap for type checking: `_bootstrap.pyi`.
Referenced by `pyproject.toml` (`mypy_path = ["src", "typings", "../.."]`). Keep stubs in sync with source APIs.
